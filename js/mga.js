//Compute optimal MGA trajectories
class MGAFinder {
	constructor(system) {
		this.system = system;
	}

	findtransfers(planet1, planet2, t1, relvel) {
		// Finds valid transfers from planet1 to planet2 starting at t1
		// with a relative velocity of relvel
	}
};